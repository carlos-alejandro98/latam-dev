import { Stack } from 'expo-router';

/**
 * Turnaround module layout.
 * Groups all turnaround screens.
 */
export default function TurnaroundLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
